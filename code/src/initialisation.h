#ifndef __INI__
#define __INI__

#include "rectangle.h"
#include "points.h"

void lectureParams(std::string &fichier, int &n, double &taux);
void initialisation(std::vector<Point> &listePoints, Rectangle &surface, std::vector<double> &r);

#endif
